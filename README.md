# dynu_ip_update
Dynu IP Update Script

Simple script to update the Dynu A record for a host with the IP of the current machine.

The host name is retrieved from the configuration file dynu.cfg located in the same directory.

The configuration file also contains the Dynu API token for making the relevant API calle for updating the record.

## Installation

## Running the script
